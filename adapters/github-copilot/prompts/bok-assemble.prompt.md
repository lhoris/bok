---
mode: agent
description: 특정 작업을 위한 Context Pack(관련 지식 + gaps)을 만든다.
---
goal에서 핵심 용어·need를 뽑은 뒤 `bok assemble --scope <ctx> --goal "<goal>"`을 실행하라. 결과의 units(L2/L3) + warnings(저confidence) + **gaps(모르는 것)** 를 제시하라. gaps가 크면 작업 전 discover를 권고. gaps를 숨기지 마라 — AI는 무엇을 모르는지 알고 작업해야 한다.
