---
mode: agent
description: 발굴된 지식을 구조화한다 — 타입 확정, ADR/arc42 본문, 관계, coverage 매핑.
---
scope에 대해 `bok context --scope <ctx>`와 `bok compile`을 실행한 뒤, 애매한 타입(kind/layer/context)을 확정하고 kind별 본문을 작성하라(reference→arc42/C4, explanation→ADR: Context·Decision·Consequences+대안 필수). `## TL;DR` 필수, 근거 있는 관계 추가. dangling relation은 미발굴 대상으로 기록.
