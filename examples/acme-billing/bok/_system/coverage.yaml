# Coverage grid — arc42 12 + TDD (design/04 B.2).
# AUTHORED input: {id, criticality, kus, open_gap}. `status` is COMPUTED by
# `bok ready` from criticality + KU confidence (+ open_gap). Do not hand-set status.
scope: billing
areas:
  - id: context-and-scope          # arc42 §3 / C4 Context
    criticality: high
    kus: [bok://billing/reference/settlement-batch]
    open_gap: true                 # 컨테이너/외부연동 KU 미완 (known-incomplete)
  - id: business-rules             # arc42 §4/§9 + business process archaeology
    criticality: critical
    kus: [bok://billing/explanation/double-settlement-guard]
  - id: data-model                 # arc42 §5 / data layer
    criticality: critical
    kus: []                        # ledger-store 미발굴 (catalog dangling과 일치)
  - id: runtime-behavior           # arc42 §6
    criticality: high
    kus: []                        # 부분 실패 재개 미확인
  - id: deployment-ops             # arc42 §7
    criticality: normal
    kus: []
  - id: decisions-rationale        # arc42 §9 / ADR
    criticality: high
    kus: [bok://billing/explanation/double-settlement-guard]
  - id: glossary-ubiquitous-lang   # arc42 §12 / DDD
    criticality: normal
    kus: [bok://billing/glossary/idempotency-key]
  - id: risks-tech-debt            # arc42 §11 / TDD
    criticality: high
    kus: []
  - id: dependencies-eol           # TDD
    criticality: normal
    kus: []
  - id: team-bus-factor            # TDD 팀 의존성
    criticality: high
    kus: []                        # double-settlement-guard가 human 단일 소스(kim) 의존
